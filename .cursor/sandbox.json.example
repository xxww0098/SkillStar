{
  "_comment": "Copy to sandbox.json to customize. Cursor sandbox example config. Verify current fields against live Cursor terminal docs before expanding it.",
  "networkPolicy": {
    "default": "deny",
    "allow": [
      "registry.npmjs.org",
      "registry.yarnpkg.com",
      "pypi.org",
      "files.pythonhosted.org",
      "*.githubusercontent.com",
      "*.github.com"
    ]
  }
}
