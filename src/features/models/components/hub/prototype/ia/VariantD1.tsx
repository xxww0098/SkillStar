/**
 * D1 — Matrix · Agent additive
 * Keep Provider × Agent matrix (B2b). Agent-specific "加法" hangs on the cell /
 * agent settings (Claude mapping), never as a shared Provider-editor tab.
 */
export { VariantB2b as VariantD1 } from "../matrix/rich/VariantB2b";
