# SkillStar

SkillStar 管理可安装、部署、分享和持续更新的 Skill。本词汇表统一产品与代码讨论中具有领域含义的名称。

## 共享与订阅

**共享频道**：
由一名或多名发布者维护、可被其他用户持续订阅的一组版本化 Skill。
_Avoid_: 共享盘、同步盘

**频道所有者**：
拥有共享频道管理权，可以管理成员、频道设置和发布权限的用户。
_Avoid_: 主用户、超级发布者

**发布者**：
有权维护共享频道内容并创建发布版本，但不一定拥有成员管理权的用户。
_Avoid_: 主用户、管理员

**订阅者**：
加入共享频道并接收其 Skill 更新、但不向频道写回修改的用户。
_Avoid_: 其他用户、从用户

**共享邀请**：
发布者向指定用户发出的、等待对方接受后授予共享频道订阅资格的请求。
_Avoid_: 分享码、对象存储信息

**订阅 Skill**：
安装在订阅者本地、来源和更新版本由共享频道管理的 Skill。
_Avoid_: 共享 Skill、本地 Skill

**本地分歧**：
订阅者对订阅 Skill 所做的、尚未来自共享频道版本的本地修改。
_Avoid_: 冲突、脏文件

**本地副本**：
从订阅 Skill 派生、由订阅者独立拥有且不再跟随共享频道更新的 Skill。
_Avoid_: 备份、共享副本

## MCP 安装

**安装计划**：
打开安装向导时一次算出的确认负载：将要执行的完整命令、`PATH` 解析出的绝对路径、全部运行时形态候选、每个表单字段的完整 Input 语义与密钥策略。
_Avoid_: 安装草稿、安装预览

**安装答案**：
用户为安装计划里某一个输入填写的值，按「scope + 该 scope 内的序号 + 变量名」寻址，不按字段名——两个没有 `valueHint` 的位置参数共用同一个名字。
_Avoid_: 表单字段、参数值

**运行时形态**：
同一个 server 发布的一种可运行方式（远端 streamable HTTP / SSE，或 npm / PyPI / OCI / mcpb 包），按本机可用性与安全性排序后由用户选定。
_Avoid_: 传输方式、包类型

**来源指纹**：
安装时写进条目的四个来源字段（registry 全名、安装版本、来源 id、运行时形态），用于更新检测、弃用提示和「已安装」判定，不依赖 server 名字字符串。
_Avoid_: 版本号、元数据

**目录源**：
一个提供 MCP 目录行的来源（官方 registry、GitHub 镜像、用户添加的 registry URL 或本地 JSON 目录文件），可单独开关，各自有独立的同步状态。
_Avoid_: 仓库、市场
