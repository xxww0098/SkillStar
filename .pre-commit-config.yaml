repos:
  - repo: local
    hooks:
      - id: skillstar-security-scan
        name: SkillStar Security Scan Gate
        entry: scripts/security_scan/precommit_security_scan.sh
        language: system
        stages: [pre-commit]
        pass_filenames: false
        always_run: true
