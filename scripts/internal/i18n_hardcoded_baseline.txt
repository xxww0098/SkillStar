# Grandfathered hardcoded-CJK debt, one `path:count` entry per file (see
# check_i18n_hardcoded.sh). The guard FAILS when a listed file's hit count
# goes UP or a brand-new file shows up with hits, but only WARNS at or below
# the count recorded here — so the ratchet only tightens. As strings move
# into src/i18n/locales/{en,zh-CN}.json, lower the count (or delete the line
# entirely once it hits 0); the guard will flag stale entries that already
# read 0.
#
# Updated 2026-08-04: cleared migrated usage/settings AI strings; baseline
# now tracks remaining intentional bilingual keywords + sharded devMock
# sample content (Chinese fixture copy for browser-dev).
#
# Updated 2026-08-14: the Models hub promoted `hub/prototype/` onto the
# production path (Models.tsx -> ModelsHub -> ia/VariantD1). Everything that
# path reaches was migrated into the locale files. The four entries below are
# the remainder, which production cannot reach: `ia/VariantD2` and
# `ia/VariantD3` only render behind `?variant=D2|D3`, which
# `isAlternateIaPrototype()` gates on `!import.meta.env.PROD`, and
# `ModelsHubPrototype`/`PrototypeOverlays` are that switcher's own chrome.
# `devMock/models.ts` is Chinese fixture copy, same as its sibling devMocks.
src/components/layout/CommandPalette.tsx:9
src/components/shared/ProviderBrandIcon.tsx:3
src/features/models/components/hub/prototype/ModelsHubPrototype.tsx:2
src/features/models/components/hub/prototype/PrototypeOverlays.tsx:1
src/features/models/components/hub/prototype/ia/VariantD2.tsx:12
src/features/models/components/hub/prototype/ia/VariantD3.tsx:14
src/features/settings/sections/BackgroundRunSection.tsx:2
src/features/usage/components/SubscriptionEditDialog.tsx:3
src/features/usage/components/UsageGrid.tsx:2
src/features/usage/lib/usageLabels.ts:3
src/lib/ipc/devMock/marketplace.ts:2
src/lib/ipc/devMock/mcpData.ts:2
src/lib/ipc/devMock/models.ts:1
src/lib/ipc/devMock/modelsData.ts:1
src/lib/ipc/devMock/skillsData.ts:4
src/lib/ipc/devMock/usageData.ts:8
